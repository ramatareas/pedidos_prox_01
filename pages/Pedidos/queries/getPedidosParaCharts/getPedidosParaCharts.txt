SELECT 
    p.fecha_plazo,
    me.estado_interno,
    me.orden_visual
FROM 
    pedidos p
JOIN 
    maestro_estados me ON p.estado_id = me.id
WHERE 
    -- Excluye los IDs de estado 9 (entregado), 10 (finalizado) y 11 (cancelado)
    p.estado_id NOT IN (9, 10, 11)