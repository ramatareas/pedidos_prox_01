UPDATE public.pedidos
SET 
  fecha_plazo = {{ datePlazo.selectedDate ? "'" + datePlazo.selectedDate + "'" : null }},
  id_compra_cliente = {{ camrefcliente.text ? "'" + camrefcliente.text + "'" : null }},
  asignado_a_id = {{ selAsignadoA.selectedOptionValue ? selAsignadoA.selectedOptionValue : null }},
  
  -- LÍNEA AGREGADA CRÍTICA --
  cliente_id = {{ selClienteCambio.selectedOptionValue ? selClienteCambio.selectedOptionValue : null }}
  
WHERE id = {{ appsmith.store.currentPedidoId }};